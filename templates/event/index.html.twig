{% extends 'base.html.twig' %}

{% block content %}
    <div class="text-center">
        <div class="card-body">
            <h1 class="card-title">{{ site_name }}</h1>

            {{ form(form) }}
        </div>
    </div>
{% endblock %}

{% block stylesheets %}
    {{ encore_entry_link_tags('event/index') }}
{% endblock %}

{% block javascripts %}
    {{ encore_entry_script_tags('event/index') }}
{% endblock %}
